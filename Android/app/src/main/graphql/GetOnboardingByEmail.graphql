query GetOnboardingByEmail($email: String!) {
    getOnboardingByEmail(email: $email) {
        id
        titleId
        title {
            id
            url
            verified
        }
        displayPictureId
        displayPicture {
            id
            email
            url
            verified
        }
        supportDocId
        supportingDoc {
            id
            url
            verified
        }
    }
}